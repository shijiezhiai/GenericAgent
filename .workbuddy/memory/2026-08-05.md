# 2026-08-05

## 工作区页面：添加系统文件选择弹窗选择本地目录

需求：工作区页面（`.page--wsm`）添加工作区时，应允许通过系统文件选择弹窗选本地目录。

### 根因/约束
- 主窗经网桥 `http://127.0.0.1:14168/` 加载，Tauri **不向该 webview 注入 JS API**，故 `window.__TAURI__.core.invoke('pick_folder')` 不可达（前端 `AT_BROWSE` 的 rfd 路径是死代码，仅 HTML fallback 生效）。
- 既有 `pick_folder` Tauri 命令（`rfd::FileDialog::pick_folder()`）无法被前端调用 → 必须走网关 HTTP 暴露，沿用 `WIN_CTRL`/`set_window_control` 的「Tauri setup 注册闭包 + 网关路由」同一模式。

### 改动
1. `frontends/desktop_gateway/src/lib.rs`
   - 新增 `FOLDER_PICKER` OnceLock + `set_folder_picker()` / `call_folder_picker()`。
   - 新增 `pick_folder_handler`：`call_folder_picker()` 返回 `{path}`；取消/未注册返回 `{path:null}`。
   - 新增路由 `GET /dialog/pick_folder`（置于 `/__win/{action}` 之后，避免落 fallback 代理）。
2. `frontends/desktop/src-tauri/src/lib.rs` setup 内
   - `ga_desktop_gateway::set_folder_picker(Arc::new(...))`：闭包用 `app_handle.run_on_main_thread` 调 `rfd::FileDialog::new().pick_folder()`，`mpsc` 通道回传路径（确保 NSOpenPanel 在主线程，避免网关 async worker 线程阻塞死锁）。
3. `frontends/desktop/static/index.html`（`wsm-addbar` 内 input 后）
   - 新增浏览按钮 `#wsm-browse`：图标 `<span data-ga-icon="folderNotchOpen">` + 独立 `<span data-i18n="wsm.browse">`（文本放子 span 是因 `applyI18n` 会 `textContent=` 覆盖，直接放按钮会清掉图标）。
4. `frontends/desktop/static/app.js`
   - i18n：`wsm.browse` = '浏览…' / 'Browse…'（zh/en 两处）。
   - `initWorkspacePage` IIFE：取 `browseBtn`，点击 `fetch(`${BRIDGE_ORIGIN}/dialog/pick_folder`)`，拿到 path 填入 `addInput`。失败静默，用户仍可手输。

### 验证
- `cargo check -p ga-desktop-gateway` 与 `cargo check -p ga-desktop` 均通过（仅既有 warning）。

### 部署注意（铁律）
- **Rust 改动属二进制级**：须重编 `ga-desktop` + 同步三处（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/Resources/runtime/app/`）+ codesign；改后进程才生效。
- **前端静态改动（index.html/app.js）只需浏览器重载**即生效（前提是运行态 app-support 的 static 已同步）。

### 协调决策（用户 09:42）
- 另有**另一个会话也在改动桌面端**。用户决定：先把本次工作区页面代码改动**暂存**，等对方改完后**统一**做重编 `ga-desktop` + 三处同步 + 重签名，避免重复构建/互相覆盖。
- 故本次**未执行**构建与部署。状态：代码就绪 + 编译通过，待统一打包。

## GA 桌面端 Overlay 标题栏修复（用户反馈"仍然不行"）

### 根因
- 用户截图里实际是在 **Chrome 浏览器** 中访问 `http://127.0.0.1:14168/`，而前端此前用 `location.hostname` 判断桌面端，导致浏览器也被加上 `.tauri-desktop`：品牌文字被隐藏、左侧出现 92px 占位、双击顶部会发 `/__win/toggle_maximize` 但无法控制浏览器窗口。
- 即使切换到原生 Tauri 窗口，Tauri `titleBarStyle: "Overlay"` 仍会显示窗口标题文字 "GenericAgent"，与内容区视觉冲突。

### 改动
1. `frontends/desktop/src-tauri/tauri.conf.json`
   - main 窗口 `userAgent`: `"GenericAgentDesktop/2.0 (Tauri; macOS)"`（浏览器无法伪造）。
   - main 窗口 `title`: `""`（Overlay 模式下去掉标题文字）。
2. `frontends/desktop/static/index.html`
   - 桌面端检测改为 `(navigator.userAgent || '').indexOf('GenericAgentDesktop') !== -1`。
   - 双击最大化仅在 `isDesktop()` 为 true 时注册，浏览器访问不再误发窗口控制请求。
   - CSS 缓存版本 `styles.css?v=255` → `?v=256`。

### 部署与验证
- 重编 `ga-desktop`（cargo build --release --offline）+ 替换 `/Applications/GenericAgent.app/Contents/MacOS/ga-desktop` + `xattr -cr` + `codesign --force --deep --sign -`。
- 三处 index.html 同步（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/Resources/runtime/app/`），md5 一致。
- 验证 `/__win/toggle_maximize` 200，窗口 bounds 从 `1280x800` 变为 `1920x1050`，确认原生窗口最大化生效。
- 原生窗口截图确认：无独立标题栏、交通灯浮在内容上方、无 "GenericAgent" 标题文字、品牌区与交通灯无重叠。

## 项目资产：支持引用本地目录（原型）

### 结论先行
- GA「项目」内部两类桶：**资产(assets)**= 项目活代码/工作目录（含 `workspace` 只读特殊资产）；**资料库(library)**= 只读参考材料（网页/PRD/文档）。多个代码 repo 应放**资产**，不是资料库。
- 原缺口：一个项目只能引用**一个**外部本地目录（`workspace`，走全局 registry、资产视图里只读）。增补「多本地目录引用」作为可编辑资产。

### 改动（仓库源文件，原型已写、语法校验通过）
1. `frontends/desktop_bridge.py`
   - 存储：新增 `_read_asset_refs`/`_write_asset_refs`，落 `.asset_refs.json`（复用 `manager.project_meta_load/save`，DB 优先+文件镜像，与 library 一致）。
   - `project_assets_handler` 响应加 `refs` 字段。
   - 新增 `project_asset_ref_add_handler`(POST `/projects/{name}/assets/ref`)、`project_asset_ref_delete_handler`(DELETE `.../ref/{id}`)、`project_asset_ref_patch_handler`(PATCH `.../ref/{id}`，支持重命名 + edit/read 模式切换)。
   - 安全校验 `_asset_ref_reject_reason`：解析符号链接后拒绝「GA 自身目录 / 项目目录自身 / 系统敏感目录(/System,/usr,/bin,/sbin,/private/var/db)」；同名路径去重。
   - 路由注册：3 条 ref 路由（assets 现有路由之后）。
2. `frontends/desktop/static/app.js`
   - `_filteredAssets` 把 `refs` 作为资产行（type='ref'，mode edit/read 决定 readonly）。
   - 资产操作栏加「📂 添加本地目录」按钮，复用 `/api/pick-folder` 原生目录选择。
   - ref 行渲染（📂 图标、来源「引用」、筛选「本地引用」）、行操作「在访达中显示」(`/path/open` mode=reveal) + 删除；i18n 补 `ph.asset.addref/ref/ref.readonly/source.ref/reveal/filter.ref`。

### 未做（明确留给下一步）
- **agent 感知**：refs 路径尚未同步进 `agent._ga_project_mode_workspace_path`（line ~1553），`code_search`/`repo_index` 目前只能打到项目目录与单个 workspace，多 ref 需把 refs 暴露给 agent（挑一个 primary 顶替 workspace 路径，其余经上下文）。
- 前端 ref 行「重命名」按钮（PATCH handler 已就绪，UI 未接）。

### 部署注意（铁律）
- 改动落在仓库源文件 `frontends/desktop_bridge.py` 与 `frontends/desktop/static/app.js`；运行态还有 `frontends/desktop/runtime/app/...` 与 `frontends/desktop/src-tauri/runtime/app/...` 两份副本（构建时由源同步覆盖）。
- 后端 py 改动 + 前端 static 改动：需同步三处（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle）+ 清 pyc + 重启内核/前端重载才生效。本次仅完成原型代码，未部署。

## 工作区浏览按钮 bug 修复（用户 10:01 报：① 显示字面量 "wsm.browse" ② 点击无弹窗）

### 根因（实测，非源码 bug）：运行态静态文件不一致 = 部分部署遗留
- 运行中网关(14168)：`index.html` 含 `wsm-browse` 按钮 ✅；但运行 `app.js` 既无 `wsm.browse` i18n 也无 `/dialog/pick_folder` 调用（计数 0）。
- 三处 app.js md5：源码 `70bd…`、app-support/bundle 均为旧版 `9c88…`（= 源码减我 wsm 改动，仅差 19 行、同 `@ga-version v339`，**不含另一会话内容** → 同步安全）。
- 运行二进制 mtime 09:52 > 我 Rust 改动 09:35 → 二进制**已含** `/dialog/pick_folder` 路由 + `set_folder_picker` 闭包（curl 该路由 200 印证）。
- bundle 签名此前已失效（`a sealed resource is missing or invalid`），应用照常运行 → 改 app.js 不会让启动更糟。
- 关联：另一会话的 Overlay 标题栏修复在 09:52 重编并部署（见上节），其构建包含了我的 Rust 改动，但其 app.js 同步版本缺我的 wsm 前端改动 → 造成不一致。

### 修复（轻量，未重编二进制）
1. `cp` 源码 `frontends/desktop/static/app.js` → app-support + bundle 两处；三处 md5 一致 `6a4b9ae…`。
2. `xattr -cr` + `codesign --force --deep --sign -` 重封 bundle（`codesign -v` 退出 0，恢复有效）。
3. 复测：运行中网关 app.js 已含 `wsm.browse`(3) 与 `dialog/pick_folder`(2)，按钮在。
- 用户需**重载页面**（Cmd+Shift+R 硬刷新）加载新 app.js；重载后按钮显示「浏览…」、点击弹系统目录选择框。
- 本次仅同步已编好的静态文件 + 重签，未触碰二进制，也未引入另一会话冲突。与另一会话的**统一重编构建**仍按计划在其完成后做。

## 项目资产引用：agent 感知 + 前端重命名 + 部署（a→c→b，用户指定顺序）

此前原型（同日志上节）已写后端 3 handler + 前端「添加本地目录」，但缺 agent 感知与重命名按钮，且未部署。本次补全并部署。

### 新增改动
1. `frontends/desktop_bridge.py` `make_agent`（~1547）
   - 读项目 `.asset_refs.json`，收集 edit 模式 ref 路径 → `agent._ga_project_mode_ref_paths`。
   - **无 workspace 时，用首个 edit 模式 ref 顶替 `_ga_project_mode_workspace_path`**（即 handler.cwd），使 `code_run`/`code_search`/`repo_index` 默认落在第一个 repo。
2. `plugins/project_mode.py`
   - 新增 `_asset_refs_block(name)`：读 `.asset_refs.json` 生成「项目资产引用（本地目录）」清单（[可写]/[只读] 标签 + 绝对路径）。
   - `_build_injection` 每轮项目上下文注入该清单 → agent 知道去哪些路径搜、哪些可改。
3. `frontends/desktop/static/app.js`
   - ref 行操作区加「✎ 重命名」按钮（与既有 📂/🗑/👁 图标风格一致）。
   - 新增 `_assetRenameRef(row)`：调 PATCH `/projects/{name}/assets/ref/{id}` 改名（复用 showPromptDialog 预填旧名）。
   - i18n 补 `ph.asset.rename` / `ph.asset.rename.prompt`。

### 部署（已执行）
- 同步三处（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/runtime/app/`）：`frontends/desktop_bridge.py`、`plugins/project_mode.py`、`frontends/desktop/static/app.js`，三处 md5 一致校验通过（desktop_bridge 95c2…、project_mode fd3d…、app.js eccf…）。
- 清 `__pycache__` 中 `desktop_bridge*` / `project_mode*` pyc。
- 重启内核：`POST /kernel/restart`(14168) → generation 1→2，up:true；内核进程重建 pid 54222。
- 端到端验证（项目 Iceberg）：POST 添加（path 经 realpath 解析、mode=edit）→ GET refs 字段含该 ref → PATCH 改名生效 → DELETE 移除；全程无痕（已删留下的空 `.asset_refs.json`）。

### 设计要点
- agent 感知走「注入项目上下文 + cwd 顶替」两路，**无需改 `ga.py` 工具实现**（code_search/repo_index 路径参数驱动，LLM 拿到 ref 绝对路径即可搜）。
- 引用进**资产**（可写活代码），`mode=read` 标只读参考；与**资料库**（只读知识）语义始终分离。

### 验证命令备忘
- 网关状态：`curl --noproxy '*' -s http://127.0.0.1:14168/kernel/state`
- 项目列表：`curl --noproxy '*' -s http://127.0.0.1:14168/projects`
- 添加引用：`curl --noproxy '*' -X POST .../projects/<name>/assets/ref -H 'Content-Type: application/json' -d '{"path":"/abs/dir"}'`

## 双击最大化触发区扩大为「整个上方空白」（用户纠正：不是不能双击，是双击错位置）

### 需求纠正
- 用户原话：「不是不能双击最大化，是我双击错位置了。应该要整个上方空白处双击都可以最大化」。
- 旧触发只匹配 `.brand`/`.page-top` 类（中心 run-state 按钮被排除），用户点到的顶部空白未命中 → 改几何判定。

### 改动（frontends/desktop/static/index.html 顶部 IIFE）
- 双击 handler 由 `e.target.closest('.brand,.page-top')` 改为**几何判定**：`e.clientY <= TOP_ZONE(=56)` 且非交互控件（button/a/input/select/textarea/.run-state/.brand-logo）即 `fetch('/__win/toggle_maximize', POST)`。
- 覆盖整条顶带（header 28px + 上方 margin），任意空白双击均可最大化/还原；交互控件（含 run-state、品牌 logo）不触发。
- 同时**移除**上一轮 DEBUG 浮层与 `http://127.0.0.1:8899/log` 上报代码。仅前端静态改动，无需重编 Rust/codesign。

### 部署与验证
- 三处 index.html 同步（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/Resources/runtime/app/`），md5 一致 `cfbc5287…`。
- 重启 GA（kill ga-desktop+kernel → `open /Applications/GenericAgent.app`）。
- curl 网关根：含 `TOP_ZONE`(3)、`toggle_maximize`(1)，无 `8899`(0) → 生效。
- `POST /__win/toggle_maximize` 两次均 200 → Rust 窗口控制链正常。`/tmp/dbg_server.py` 已删。

### 备注
- `TOP_ZONE=56` 覆盖 header(28px)+上方约 28px 内容。若顶部消息列表双击也最大化且觉得烦，可收窄到仅 header(≈30px)；反之可调大。

## 修复：拖动窗口能力丢失（双击最大化改造后用户反馈"拖动窗口的能力没有了"）

### 根因
- 此 webview 经网桥(http://127.0.0.1:14168)加载，Tauri **不注入** `window.__TAURI_INTERNALS__`。
- 原生 `data-tauri-drag-region` 属性在 mousedown 时会调用 `window.__TAURI_INTERNALS__.invoke('plugin:window|start_dragging')`（见 tauri-2.11.1/src/window/scripts/drag.js），因 internals 不存在 → 原生拖拽在此 webview 完全失效。所以顶带区域的实际鼠标事件全被 webview 接收，macOS 原生 Overlay 标题栏拖拽也接不住（8899 日志曾实证 y=12-13 事件进 webview）。
- 结论：拖动必须走网关，与双击最大化同一模式。

### 改动
1. `frontends/desktop/src-tauri/src/lib.rs` WIN_CTRL 闭包
   - 新增 `"start_drag"` 分支：`handle.run_on_main_thread(move || { w.start_dragging() })`。必须 main thread（AppKit 限制）；按键在 mousedown+move 期间仍按住，调用即进入系统拖动。
   - `start_dragging` 经 `dispatcher` 调 WRY/macOS，Rust 内部调用绕过 capability 权限系统（无需改 capabilities）。
   - 网关 `POST /__win/{action}`（any 路由）已把 action 透传给 WIN_CTRL → 无需改网关路由。
2. `frontends/desktop/static/index.html` 顶部 IIFE
   - 原"双击即最大化"改为"拖拽+双击"合并判定：mousedown 在顶带(TOP_ZONE=56,非交互)记录起点；mousemove 位移>4px → `fetch('/__win/start_drag')` 发起拖动；mouseup 无位移且距上次点击<350ms → `fetch('/__win/toggle_maximize')` 最大化/还原。
   - 拖拽与双击用移动阈值分离，互不误触；顶带空白处 `e.preventDefault()` 防文本选中。交互控件(run-state/按钮/logo)仍被排除。

### 部署与验证
- 重编 ga-desktop：`env PATH=.../CommandLineTools/usr/bin:... SDKROOT=.../MacOSX.sdk LIBRARY_PATH=/usr/lib cargo build --release --offline`（src-tauri 下）。首编 E0505（app 既借又 move）→ 改用 `let app2 = handle.clone(); handle.run_on_main_thread(move || { app2... })` 修复，二编通过。
- 部署二进制：`cp target/release/ga-desktop → /Applications/GenericAgent.app/Contents/MacOS/ga-desktop`；`xattr -cr`；`codesign --force --deep --sign -`；`codesign -v` OK。
- 三处 index.html 同步（源码仓/app-support/bundle），md5 一致 `865e5d31…`。
- 重启 GA。验证：`POST /__win/start_drag`=200、`POST /__win/toggle_maximize`=200、网关根含 `start_drag`(2)/`toggle_maximize`(1)/无 `8899`。

### 备注（待用户实测）
- 路由/闭包已实证可达；真实拖动需在按住左键+移动时由 `start_dragging` 接管。若实测拖动仍无效，最可能原因：macOS `start_dragging` 要求严格在 mouse 事件内调用，而 `run_on_main_thread` 有微小延迟。备选方案：前端在 mousedown 即发 start_drag（不等 move 阈值），或在 drag.js 注入路径上做文章（但 internals 缺失，原生拖拽不可用）。

## 优化：左上角 G logo 与交通灯间距过宽

### 用户反馈
- 用户截图反馈「这个位置太丑了」：左上角交通灯右侧的橙色 G logo 因避让 padding 过大，与交通灯之间留白太多，显得空旷。

### 改动
- `frontends/desktop/static/styles.css` line 915
  - `.tauri-desktop .brand { padding-left: 92px; }` → `padding-left: 76px;`
  - 注释更新为「避让交通灯(16+52+8 紧凑余量)」。
  - 仅改 CSS，无需重编 Rust 二进制。

### 部署
- 三处 styles.css 同步（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/Resources/runtime/app/`），md5 一致 `f40e1bde…`。
- 重启 GA。curl 网关 `/styles.css` 确认已下发 `padding-left:76px`。

### 备注
- 若 76px 仍显空或太挤，可继续微调（建议范围 72-84px）。

## 修正：左上角 logo 间距（76px 实测交通灯被挤不完整）
- 76px（16+52+8）实测导致交通灯显示不完整（logo 太贴近绿灯）。
- 改回 84px（16+52+16，约 16px 间隙）：比原 92px 紧凑 8px，且保证三灯完整。
- 三处 styles.css 同步（md5 `f122bbe6…`），重启生效，网关已确认下发 `padding-left:84px`。

## 优化：改为原生 App 图标+文字顶栏布局

### 用户反馈
- 用户发参考图：交通灯右侧显示「头像/图标 + 姓名/应用名」。
- 用户说 84px 时交通灯仍不完整，希望 GA 改为：交通灯 + GA 图标 + "GenericAgent" 文字。

### 改动
1. `frontends/desktop/static/styles.css`
   - `.tauri-desktop .brand { padding-left: 92px; }`：回到足够余量，确保交通灯完整。
   - 新增 `.tauri-desktop .brand-name { font-size:14px; color:var(--txt-2); }`：顶栏文字更精致。
   - 隐藏选择器从「隐藏 brand-name/brand-sub/ga-wordmark」改为「只隐藏 brand-sub 和不含 brand-name 的 ga-wordmark」，使 `.brand-name.ga-wordmark` 显示。
2. `frontends/desktop/static/index.html`
   - `<div class="brand-name ga-wordmark">Generic Agent</div>` → `GenericAgent`（去掉空格，与应用名一致）。

### 部署
- 三处 styles.css + index.html 同步，md5 一致；已重启 GA。
- 网关确认：CSS 含 `padding-left:92px`、`.tauri-desktop .brand-name` 样式；HTML 含 `GenericAgent`。

### 备注
- sidebar 默认宽度 232px，brand 文字区可用宽度约 98px；"GenericAgent" 14px 下约 90-100px，刚好放下。若用户 sidebar 收窄可能截断，可再调小字号到 13px 或 sidebar 最小宽度。

## 根因定位+修复：左上角交通灯「显示不完整」反复出现

### 根因（关键！）
- 此 webview 经网桥加载，macOS 交通灯位置由 Tauri 在**编译时**从 `tauri.conf.json` 的 `trafficLightPosition` 嵌入 binary；运行时改 conf 文件不生效。
- 之前运行的 binary 是用**旧配置**编译的，交通灯实际落在 webview x≈95（非配置的 16）。我此前所有 `padding-left` 调整（76/84/92）都 < 95，导致橙色 GA logo 直接压在交通灯上 → 用户看到的「交通灯不完整」「图标文字太靠右」。
- 用 `screencapture -l <CGWindowID>` + PIL 像素分析实测：旧 binary 红灯在 x≈108（截图）/95（webview）。

### 修复
1. **重编 binary**：`tauri.conf.json` 已有 `trafficLightPosition:{x:16,y:8}`，重编后该值编译进 binary，交通灯回到标准左上角。实测红灯16/黄灯39/绿灯62-75。
   - 编译：`env PATH=.../CommandLineTools/usr/bin:... SDKROOT=.../MacOSX.sdk LIBRARY_PATH=/usr/lib cargo build --release --offline`（src-tauri 下）。
   - 部署：cp target/release/ga-desktop → /Applications/GenericAgent.app/Contents/MacOS/ga-desktop；`xattr -cr`；`codesign --force --deep --sign -`；重启。
2. **CSS 调优**（`frontends/desktop/static/styles.css`）：
   - `.tauri-desktop .brand { padding-left:84px; }`（logo 落交通灯右 9px 间隙，紧凑像原生 app）。
   - `.brand-logo { width:22px; height:22px; border-radius:5px; }`（缩小，用户嫌太大）。
   - `.tauri-desktop .brand-name { font-size:14px; color:var(--txt-2); }` 显示 GenericAgent 文字。
   - 隐藏选择器只隐藏 `.brand-sub` 和不含 brand-name 的 ga-wordmark。
3. `index.html` 品牌文字改 `GenericAgent`（无空格）。

### 验证（像素分析，非肉眼）
- 截图 CGWindowID（Quartz 获取）→ PIL 分析：交通灯红灯16/黄39/绿62-75 完整；logo 左≈84（截图96-阴影12），交通灯右75，间隙9px 不重叠。
- 三处 styles.css + index.html 同步（md5 一致），已重启生效。

### 经验
- Tauri 配置改了必须重编 binary 才生效（conf 编译进 binary，非运行时读文件）。
- macOS 交通灯位置像素诊断法：`lsappinfo`/`Quartz` 拿 CGWindowID → `screencapture -l <id> -o` → PIL 找红(255,95,87)/黄(254,188,46)/绿(40,200,64) 聚类。DPR=1（GA 在外接非 Retina 屏时截图像素=逻辑像素）。

## 重大踩坑：二进制部署"假成功"导致用户看到"没变化"

### 现象
- 用户重启后报告"没变化"。排查发现：磁盘 `/Applications/.../ga-desktop` md5 `feaaf108…` ≠ 重编产物 `74d96b2b…`；文件大小 18,534,304 ≠ built 18,624,464。**cp 根本没覆盖成功**，但上一轮命令打印了 "copied new binary"（假象）。

### 根因
- 上一轮部署时 app 进程可能仍占用二进制文件（或签名状态干扰），`cp "$BUILT" "$DEPLOYED" && echo` 退出码看似成功、实际 inode 未替换 → 磁盘仍是旧 binary → 交通灯留在旧位置(x≈95) → 用户"没变化"。
- **md5 校验二进制是错误方法**：`codesign --force --deep --sign -` 会在二进制里写签名块、改变文件哈希（实测 built `74d96b2b` → 签名后 `cb0e0708`、`codesign --remove-signature` 也不能字节级还原成原值）。所以"去签名比对"也会误判。

### 正确部署流程（已验证）
1. `pkill -9 -f "Contents/MacOS/ga-desktop"`（及 kernel/desktop_gateway），`rm -f /tmp/com_genericagent_desktop_si.sock`，`sleep 2`，确认进程数=0。
2. `cp -f "$BUILT" "$DEPLOYED"`（强制覆盖）。
3. **立即用 `stat -f%z` 比大小**：built size 必须等于 deploy size（这是唯一可靠的"覆盖成功"判据，别用 md5）。
4. `xattr -cr /Applications/GenericAgent.app` → `codesign --force --deep --sign - /Applications/GenericAgent.app` → `codesign -v` 必须 OK。
5. 再 `open /Applications/GenericAgent.app`。

### 验证二进制是否真正生效（不看 md5，看行为/像素）
- 截图该窗口（`lsappinfo`/Quartz 拿 CGWindowID → `screencapture -x -o -l <id>`）→ PIL 扫交通灯三色红(255,95,87)/黄(254,188,46)/绿(40,200,64) 聚类。
- 新 binary（trafficLightPosition x=16）：红灯≈x16、黄≈39、绿≈62-75。旧 binary：红灯≈x95。此差异可 100% 判定二进制是否换上。

### 本次结果
- 按上述流程重做：`cp -f` 后 deploy size=18,624,464=built ✅；codesign OK；启动后截图实证交通灯红灯 x=16（新 binary 生效）。静态 CSS(三处 md5 一致 f31e59d2) 与网关下发均正常。

## 修复：左上角 "GenericAgent" 文字被截断成 "GenericAg"

### 根因
- 用户截图显示：交通灯完整，但右侧 GA 图标后的应用名被截断为 `GenericAg`。
- `.brand` 基础样式有 `overflow:hidden`，桌面端未覆盖；同时 `.brand-text` 作为 flex item 默认可被压缩。二者叠加导致文字区域被裁剪/收缩。
- 不是空间不足：sidebar 宽 232px，交通灯 + logo + 完整文字只需约 180px。

### 改动
- `frontends/desktop/static/styles.css` 桌面端覆盖：
  - `.tauri-desktop .brand` 加 `overflow:visible; width:100%; max-width:none; box-sizing:border-box; padding-left:80px;`
  - `.tauri-desktop .brand-text` 加 `flex:0 0 auto; min-width:auto; overflow:visible;`（文字区不收缩）
  - `.tauri-desktop .brand-name` 加 `overflow:visible;`

### 部署
- 三处 styles.css 同步（源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/Resources/runtime/app/`），md5 一致 `e6bd0e76…`。
- 仅静态改动，无需重编 binary；用户需重启 GA 生效。

## 补坑：改 styles.css 必须同步 bump index.html 的 CSS 版本号

### 现象
- 上一节改完 styles.css 同步三处后用户重启"没变化"。经查三处磁盘文件均已含 `padding-left:80px`，但 index.html 引用仍是 `styles.css?v=256` → webview 重启后复用缓存的旧 v=256 CSS，新内容不加载。
- **铁律**：每次改 `styles.css` 内容，必须同步把 `index.html` 里 `styles.css?v=N` 的 N bump +1，否则 webview 缓存导致"部署无效"假象。

### 本次修复
- `index.html` 第 10 行 `styles.css?v=256` → `?v=257`；三处 index.html 同步（md5 一致），均含 v=257。
- 网关(14168)实测：根页引用 v=257，`/styles.css?v=257` 响应含 `padding-left:80px` → 运行态已生效。
- **部署静态文件完整流程**：①改 styles.css ②bump index.html 版本号 ③同步三处(index.html+styles.css) ④重启 GA。纯静态改动不重编 binary、不重签。

## 追加：用户"重启了还是不行"的根因与最终验证

### 根因再定位
- 三处静态文件（styles.css + index.html）在磁盘上已是修复版（md5 `e6bd0e76…`，v=257），但用户重启后仍截断。
- 实测运行中网关(14168)当时返回的是**旧 v=256 / 截断 CSS**；磁盘文件与网关响应不一致。
- 关键发现：`GA_BUILD_ID` 克隆在**每次启动**时会把 bundle `Contents/Resources/runtime/app/frontends/desktop/static/` 覆盖到 `~/Library/Application Support/GenericAgent/app/frontends/desktop/static/`。**bundle 副本也必须保持修复版**，否则一次重启就把 app-support 改回旧文件。
- 同时 `ga-desktop` 启动器会把 `MacOS/ga-desktop` 复制到 `.bak_<ts>` 再 exec，这是为了自更新；该机制不影响静态文件，且启动后 `.bak` 会被清理。

### 最终修复
1. 重新确认并同步三处 `styles.css` + `index.html`：
   - 源码仓 `frontends/desktop/static/`
   - 运行态 `~/Library/Application Support/GenericAgent/app/frontends/desktop/static/`
   - bundle `…/Contents/Resources/runtime/app/frontends/desktop/static/`
2. 为进一步确保 webview 不命中缓存的 v=257（可能在回滚期间被缓存过），把版本号 `?v=257` → `?v=258`。
3. 无需重编 binary：`/Applications/…/MacOS/ga-desktop` 已通过 `strings` 确认内嵌 `trafficLightPosition`+`Overlay`，即交通灯 x=16 已编译进 binary。

### 验证（本次）
- 重启 GA：新进程 pid 90401。
- `curl --noproxy '*' http://127.0.0.1:14168/` 返回 `styles.css?v=258`。
- `curl --noproxy '*' http://127.0.0.1:14168/styles.css` md5 = `e6bd0e76…`，含 `.tauri-desktop .brand{overflow:visible; padding-left:80px;}` 等修复。
- `/Applications/GenericAgent.app/Contents/MacOS/` 目录无 `.bak_*` 残留。
- **视觉截图验证被阻断**：该 Tauri 窗口设置了屏幕捕获保护，`screencapture -x`/`-l` 只能捕获到黑色区域，无法像前几轮那样做像素级确认；本次以 HTTP 级响应作为生效判据。

### 结论
- 磁盘三处 + 网关下发现在都是修复后的 v=258 CSS，且 binary 已含 x=16 交通灯配置；用户应已看到完整的"GenericAgent"文字。
- 后续再改静态文件必须同时同步 bundle，不能只改 app-support；否则下次重启会被 bundle 克隆覆盖。

## 再修正："GenericAgent" 仍被截断成 "GenericAgen"（真正的根因）

### 现象
- 上文以为"v=258 + overflow:visible 已修复"，但用户继续反馈不行。本次用 `screencapture` 成功拍到实际窗口，发现文字仍被截断为 "GenericAgen"（末尾 "t" 缺失）。

### 真正根因
- 基础 `.brand` 样式里有 `flex: 0 0 var(--shell-head-h);`（`--shell-head-h=28px`），这意味着 `.brand` 容器在父 flex 布局中被固定为 **28px 宽**。
- 此前只改了 `overflow:visible`、`width:100%`、`brand-text flex:0 0 auto` 等，都没有覆盖这个 `flex-basis`，所以容器宽度仍只有 28px，文字自然被 sidebar 的 `overflow:hidden` 裁掉。
- 此前靠 `grep -a trafficLightPosition` 判断 binary 已编译新配置是**错误判据**：这些字符串是 Tauri 的 serde 字段名表，任何 Tauri 二进制都会出现，不能证明配置生效。

### 最终改动（frontends/desktop/static/styles.css）
- `.tauri-desktop .brand`：
  - `padding-left: 80px` → `58px`（交通灯实际占位没那么宽，减少空白）。
  - 新增 `flex: 0 0 auto !important;`（**关键**：覆盖基础 28px 固定宽度）。
  - `width: 100%` → `width: auto;`（让宽度由内容决定）。
- `.tauri-desktop .brand-name`：`font-size: 14px` → `13px`（sidebar 宽度有限，13px 刚好完整显示 GenericAgent）。

### 部署
- `styles.css?v=258` → `?v=260`（中间试过 v=259 仍不够，最终 v=260）。
- 三处同步：源码仓 / `~/Library/Application Support/GenericAgent/app/` / bundle `…/Resources/runtime/app/`。
- 重启 GA，gateway 下发 v=260，md5 变为 `eacca199…`。

### 验证（截图）
- `screencapture -x -o` 全屏截图 → PIL 按 Quartz 返回的窗口 bounds 裁剪左上角。
- 结果：交通灯红/黄/绿完整显示在左上角，橙色 GA logo 紧贴交通灯右侧，"GenericAgent" 文字完整不再截断。
- 成功截图文件：`/tmp/ga_topleft3.png`。

### 经验
- 遇到 flex 子项宽度异常，优先查**基础 flex 属性**（`flex: 0 0 <length>` 会覆盖 width）。
- 不要靠二进制的字符串片段判断 Tauri 配置是否生效；应直接观察运行时行为/像素位置。
- CSS 调试时，若 `overflow:visible` 不生效，要检查**所有祖先容器**是否有 `overflow:hidden` 或固定 flex-basis。

## 交通灯与 logo/文字垂直对齐（用户："交通灯不完整、太大、和图标文字太近"）

### 可靠的窗口截图方法（重要，替代 screencapture）
- `screencapture -x` 抓全屏会拍到**遮挡 GA 的其他窗口**；`screencapture -l <winid>` 常报 "could not create image from window"。
- **正确做法**：用 Quartz `CGWindowListCreateImage(CGRectNull, kCGWindowListOptionIncludingWindow, wid, kCGWindowImageBoundsIgnoreFraming)` 直接抓指定窗口，**不受遮挡影响、无需窗口置顶**。
- 转 PIL：`Image.frombuffer("RGBA",(W,H),bytes(CGDataProviderCopyData(...)),"raw","BGRA",bytesPerRow,1)`（注意 **BGRA** 不是 RGBA）。
- scale = 图像宽 / `kCGWindowBounds.Width`（Retina=2）。
- 复用脚本：`/tmp/ga_measure.py`（输出左上角 ASCII 图 + 交通灯/logo/文字的 x,y 段与间隙、垂直中心差）。
- 注意：**当前模型读不了图片**，必须靠 ASCII/像素段数值判断，不能"看图说话"。

### 实测数据（修复前）
- 交通灯三颗：x=16~29 / 39~52 / 62~75（直径约14px，组右边缘 **75**），**y=2~14，中心 y≈8**。
- GA logo：x=69~91，**y=18~39，中心 y≈28**。
- 根因三连：① `padding-left:58px` 使 logo 左缘 x=69 撞进绿灯区间(62~75) → "不完整/太近"；② 灯中心 y=8 vs logo 中心 y=28，**垂直错开 20px** → 灯孤悬左上角显得突兀（用户说"太大"）。

### 修复
1. **`frontends/desktop/src-tauri/tauri.conf.json`**：`trafficLightPosition` `{x:16,y:8}` → **`{x:16,y:28}`**。
   - y 语义 ≈ 灯组**中心**的 y 偏移；28 = sidebar `--shell-pad`(14) + `.brand` 高(28)/2，正好对齐 brand 垂直中心。
   - **改 conf 必须重编二进制**（39s）。
2. **`frontends/desktop/static/styles.css`** `.tauri-desktop`：
   - `padding-left: 58px → 73px`（注意 sidebar 自身已有 12px 内边距，12+73=85 = 灯组右缘75 + 10px 间隙）。
   - 新增 `.tauri-desktop .brand-logo{ width:20px; height:20px; margin-right:7px; }`（从22px缩小，给文字腾宽度）。
   - `index.html` 版本 v=260 → **v=262**。

### 验证结果（像素实测）
- 交通灯 x=16~29/39~52/62~75 三颗完整，y=20~32，中心 26。
- logo x=85~104，中心 y=27.5 → **垂直中心差仅 1.5px**。
- 灯组右缘 75 → logo 左缘 85，**间隙 10px**。
- 文字 "GenericAgent" x=113~193（12 字母字形完整），sidebar 右边框 x=204，**余量 11px 未截断**。

### 遗留
- 交通灯直径 14px 由 macOS 系统决定，Tauri **无法调整尺寸**，只能调位置。

## 部署二进制的可靠姿势（本次再次验证）
- `pkill -9 -f "Contents/MacOS/ga-desktop"` → `mv` 旧二进制到 /tmp → `cp -f` 新产物（**mv 而非直接 cp，避免 cp 假成功**）→ 比对**前17MB** md5（签名段 dataoff≈18479936 之后不参与比较）→ 清 `MacOS/*.bak_*` → `xattr -cr` → `codesign --force --deep --sign -` → `codesign -v`。
- 本次前17M 哈希 `5b3f9833` 源码=部署完全一致，签名后 size 18534304。
- **zsh 坑**：`rm -f dir/*.bak_*` 在无匹配时会报 `no matches found` 并**中断整个命令链**（后续 xattr/codesign 不执行）。要么 `setopt NULL_GLOB`，要么把 rm 单独成句加 `2>/dev/null`。

## 交通灯对齐修复：重启后再次实测确认（续）
- 上一轮改 `tauri.conf.json` 的 `trafficLightPosition:{x:16,y:28}`（y 8→28）+ CSS `padding-left:73px`+`logo 20px`+`brand-name 13px`(v=262)，已重编二进制部署。
- 本次（pid 97313 全新实例）用 Quartz 直抓窗口做像素级复测，确认修复真生效：
  - 交通灯 x=16..74（三颗完整圆，无裁切）→ 解决「不完整」。
  - 灯中心 y≈25.8 vs logo 中心 y≈27.5 → 垂直差 **1.7px**，基本齐平。
  - 灯右缘 74 → logo 左缘 85 水平间隙 **11px** → 解决「太近」。
  - logo 实测 19px 宽（配置 20px），"GenericAgent" 文字此前已确认完整。
- **重要限制**：macOS 交通灯直径 ~13px 由系统决定，Tauri 无法缩小；「太大」的观感源于旧版垂直错开孤悬，现已对齐，观感应正常。若要更小只能改 macOS 系统缩放（不可取）。
- 截图 `/tmp/ga_topleft_now.png`（460x112，逻辑 230x56@2x）供用户肉眼确认。
- 复测脚本 `/tmp/ga_measure.py`(原)、`/tmp/ga_measure3.py`(隔离灯区)、`/tmp/ga_crop.py`(裁图) 均用 **系统 python3**（含 pyobjc/Quartz），managed python 无 Quartz。
