# Flink 运维专家知识库

> 本文件为 L2 知识库（按需读取，不每轮注入）。
> 详细的运行机制与指标资产，优先读 plugin 配套包：`temp/flink-diagnostics-plugin/knowledge/`
> —— `runtime-mechanism-pack`（运行机制）/ `diagnostic-principles-pack`（诊断原则）/ `metrics-reference-pack`（指标参考）/ `sql-optimization-pack`（SQL 优化）。

## 诊断经验沉淀

（暂无。每次诊断结束自问：哪条经验值得未来的我直接复用——再踩一次坑、再摸索一次？有，就追加成一句话；没有，一个字都不写。）
